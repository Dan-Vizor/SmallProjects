#include <iostream>
using namespace std;

int main() {
	char mode = "";
	char data = "";
	int key = 0;
	cout << "1 - encrypt \n 2 - decrypt" << endl;
	cout << "enter mode: ";
	cin >> mode;

	if (mode == "1" {
		cout << "enter string to encrypt: ";
		cin >> data;
		cout << "enter encryption key (2-25): ";
		cin >> key;

		for (int i = 0; i < data.length(); ++i) {
			cout << i << endl;
		} else {
			//a
		}

	return 0;
}